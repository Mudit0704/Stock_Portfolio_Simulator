package portfolio.model;

/**
 * Enum to represent the different service types that may be extended for support in the future.
 */
public enum ServiceType {
  /**
   * ALPHAVANTAGE API.
   */
  ALPHAVANTAGE
}